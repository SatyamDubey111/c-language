// What will be the value stored in the variable x after executing following statement: x=!12>2;

//  what will be the value stored in the variable x after executing folowing statement: x=!12>2;

#include <stdio.h>
int main()
{
    int x;
    // printf("Enter the number");
    // scanf("%d",&x);
    x = (!2)>(-2);
    printf("the output is the given expression x is %d",x);
    printf("\n");
    return 0;
}